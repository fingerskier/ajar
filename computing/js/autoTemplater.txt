function node(tagName, dataProxy) {
  proxy = {
    get: ...


    set() {
      el.setAttr(prop, val)
    }

  }

  el = create element

  el.addEventListener('change', 
}


node `prop1 ${fx1} prop2 ${fx2} ...`